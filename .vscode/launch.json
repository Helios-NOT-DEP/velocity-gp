{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Velocity GP (Chrome)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173/",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "npm: dev"
    }
  ]
}
