/**
 * @file index.ts
 * @description Master barrel export for the `@velocity-gp/api-client` package.
 * Exposes the reusable, isomorphic `ApiClient` abstraction designed to uniformly
 * interface with the backend Express server using the contracts defined in `@velocity-gp/api-contract`.
 */

export * from './client.js';
